const {
  select,
  selectRandomQuizzes,
  selectById,
  selectBySubjectId,
} = require("./select");
const { insert } = require("./insert");

module.exports = {
  select,
  selectRandomQuizzes,
  selectById,
  selectBySubjectId,
  insert,
};
